

// Define the 'currentUser' variable with a sample name
const currentUser = "John Doe";

// Define 'welcomeMessage'
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define 'excitedWelcomeMessage'
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Define 'shortGreeting'
const firstInitial = currentUser[0]; // Get the first character of 'currentUser'
const shortGreeting = `Welcome, ${firstInitial}!`;

// The code above defines the variables in a way that should pass the tests described in your file.
